- name: Copy systemd Service file
  template:
    src: "login.service"
    dest: /etc/systemd/system/login.service

- name: Start login Service
  systemd:
    name: "login"
    daemon_reload: yes
    state: started
    enabled: yes