- name: Copy systemd Service file
  copy:
    src: "login.service"
    dest: /etc/systemd/system/login.service
    remote_src: yes
    owner: ubuntu
    group: ubuntu
    

- name: Start login Service
  systemd:
    name: "login"
    daemon_reload: yes
    state: started
    enabled: yes